export class Studient {
  constructor(private cin: string, private fullName: string,
              private inscriptionDate: Date, private diploma: string,
              private imageURL: string, private cvURL: string) {}
}
