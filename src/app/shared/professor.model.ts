export class Professor {
  constructor(private cin: string, private fullName: string, private grade: string,
              private faculty: string, private imageURL: string,
              private cvURL: string) {}
}
